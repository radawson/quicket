services:
  db:
    image: postgres:18
    restart: always
    environment:
      POSTGRES_DB: informejo
      POSTGRES_USER: informejo
      POSTGRES_PASSWORD: PASSword01!!
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql 
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U informejo -d informejo"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  db_data: